# Implementation Plan: Assistant Chat Redesign

**Branch**: `008-assistant-chat-redesign` | **Date**: 2026-02-19 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/008-assistant-chat-redesign/spec.md`

## Summary

Redesign the assistant chat page across 10 UI/UX improvements: enriched empty state with context-specific icons/descriptions/integrated prompts, modernized input bar with icon send button and controlled clearing, assistant context controls with icons, animated typing indicator (bouncing dots via reanimated), new conversation button with confirmation, enhanced assistant message bubbles (white bg + shadow + avatar + timestamps), markdown rendering for assistant responses, scroll-to-bottom FAB, feedback action repositioned to toolbar, and visually distinct context prompt chips.

One new dependency: `react-native-markdown-display`. Four new components: `EmptyState`, `TypingIndicator`, `ChatToolbar`, `ScrollToBottomButton`. Five modified components: `ChatArea`, `MessageBubble`, `AssistantContextControls`, `ContextPrompts`, `assistant.tsx`.

## Technical Context

**Language/Version**: TypeScript 5.9.2 on React Native 0.81.5 (Expo SDK 54)
**Primary Dependencies**: Expo Router 6.0, Zustand 5.0.3, react-i18next 15.4.1, NativeWind 4.1.23, @gluestack-ui/themed 1.1.73, react-native-reanimated 4.1.1, @expo/vector-icons 15.0.3 (Ionicons), react-native-markdown-display (NEW)
**Storage**: AsyncStorage (native) / localStorage (web) via Zustand persist middleware; key `"assistant-state"` — no changes
**Testing**: Jest + React Native Testing Library (existing setup)
**Target Platform**: iOS 15+ / Android 10+ / Web (Expo managed workflow)
**Project Type**: Mobile (single Expo project)
**Performance Goals**: 60fps animations (typing indicator, scroll-to-bottom); markdown rendering under 16ms per frame during streaming
**Constraints**: Expo managed workflow (no native modules requiring ejecting); offline guard already handles network absence; minimum 44px touch targets (WCAG 2.1 AA)
**Scale/Scope**: 1 screen (assistant tab), 9 components modified/created, 1 new npm dependency

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle | Status | Assessment |
|-----------|--------|------------|
| I. Neutrality & Non-Prescription | PASS | Pure UI redesign. No content changes. Empty state descriptions use existing neutral i18n keys. Context icons are generic metaphors (book, chat, scale) — no political connotation. Context prompts are unchanged. |
| II. Source-Grounded Truth | PASS | Source display (TrustBadge) remains unchanged. Markdown rendering displays the same LLM output — only visual presentation changes. No new factual claims introduced. |
| III. City-Agnostic Architecture | PASS | All changes are in generic UI components. No Paris-specific content. Context descriptions, icons, and prompts are election-agnostic. |
| IV. Critical Thinking Over Persuasion | PASS | No changes to conversation logic, prompts, or system behavior. The assistant personalization flow and Socratic questioning are untouched. |
| V. Structured Data as Single Source of Truth | PASS | No new data sources. Existing i18n keys, ChatMessage schema, and election dataset used as-is. |
| VI. Simplicity & MVP Discipline | PASS | All changes are within the existing Assistant tab. No new tabs, screens, or navigation routes. One new dependency (markdown renderer) — justified by FR-019 requirement. Four new components are small, focused, and single-purpose. |
| VII. Privacy & Trust | PASS | No new data collection. Feedback action is repositioned, not modified. No new analytics or tracking. |

**Gate Result**: ALL PASS — no violations. Complexity Tracking not needed.

**Post-Phase 1 Re-check**: PASS — design decisions maintain all constitutional compliance. The markdown renderer is a display-only library with no network calls or data collection. The typing indicator is purely cosmetic. All new components are stateless or use existing store state.

## Project Structure

### Documentation (this feature)

```text
specs/008-assistant-chat-redesign/
├── plan.md              # This file
├── spec.md              # Feature specification
├── research.md          # Technology decisions and alternatives
├── data-model.md        # Data model analysis (no changes needed)
├── quickstart.md        # Developer setup guide
├── checklists/
│   └── requirements.md  # Spec quality checklist
└── tasks.md             # Task breakdown (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
src/
├── app/
│   └── (tabs)/
│       └── assistant.tsx              # MODIFY — remove ContextPrompts/FeedbackAction, add ChatToolbar
├── components/
│   └── assistant/
│       ├── ChatArea.tsx               # MODIFY — controlled input, send icon, separator, scroll FAB, empty state
│       ├── MessageBubble.tsx          # MODIFY — white bg + shadow, avatar, timestamps, markdown rendering
│       ├── AssistantContextControls.tsx           # MODIFY — add icons, increase text size
│       ├── ContextPrompts.tsx         # MODIFY — outlined chips, arrow icon, flex-wrap layout
│       ├── CandidateSelector.tsx      # UNCHANGED
│       ├── EmptyState.tsx             # NEW — context icon, title, description, integrated prompts
│       ├── TypingIndicator.tsx        # NEW — animated 3-dot bouncing dots (reanimated)
│       ├── ChatToolbar.tsx            # NEW — new conversation + feedback icon row
│       └── ScrollToBottomButton.tsx   # NEW — floating FAB with animated visibility
├── stores/
│   └── assistant.ts                   # UNCHANGED — resetConversation() already exists
├── services/
│   └── chatbot.ts                     # UNCHANGED — streaming mechanism untouched
└── i18n/
    └── locales/
        └── fr/
            └── assistant.json         # UNCHANGED — existing keys now surfaced in UI
```

**Structure Decision**: Existing Expo managed mobile project structure. All changes are within `src/components/assistant/` (component layer) and `src/app/(tabs)/assistant.tsx` (screen layer). No new directories needed — new components are added alongside existing ones in the `assistant/` component folder.

## Key Design Decisions

### 1. Component Architecture

**EmptyState** (new)
- Receives: `context`, `context`, `onPromptSelect`, `selectedCandidateId`
- Renders: context icon (Ionicons), context title (i18n `{context}Context`), description (i18n `{context}ModeDescription`), and `ContextPrompts` chips
- Used as `ListEmptyComponent` in ChatArea's FlatList
- Replaces: the separate `ContextPrompts` rendering in `assistant.tsx` AND the plain text `ListEmptyComponent` in `ChatArea.tsx`

**TypingIndicator** (new)
- Three `Animated.View` circles (8x8 each, bg-text-caption) with staggered `withDelay` + `withRepeat(withSequence(translateY))` for bouncing
- Wrapped in an assistant-style bubble (bg-white, rounded-2xl, shadow-card) with the sparkles avatar
- Rendered as the last item in the FlatList when `isStreaming && lastMessage.content === ""`
- Disappears when the assistant message gets its first content chunk (FR-012)

**ChatToolbar** (new)
- Horizontal row: left side = new conversation icon button (visible only when messages exist), right side = feedback icon button
- `new conversation` uses `refresh-outline` icon + `Alert.alert()` confirmation with i18n keys `newConversation` / `resetConfirm`
- `feedback` uses `flag-outline` icon → opens the existing `FeedbackAction` component inline (expanded below the toolbar)
- Positioned between AssistantContextControls and ChatArea in `assistant.tsx`

**ScrollToBottomButton** (new)
- Absolute-positioned circular button (40x40) with `chevron-down` icon
- Positioned 16px above the input bar, horizontally centered or right-aligned
- Visibility controlled by `isAtBottom` state from ChatArea's `onScroll` handler
- Uses `useAnimatedStyle` with `opacity` and `translateY` for smooth fade-in/slide-up

### 2. ChatArea Refactoring

The `ChatArea` component receives the most changes:
- **Input model**: `useRef<string>` → `useState<string>` for controlled clearing
- **Send button**: `<Text>Envoyer</Text>` → `<Ionicons name="send" />` in a circular `bg-accent-coral` button
- **Input separator**: Add `border-t border-warm-gray` or `shadow-sm` to the input container
- **Scroll tracking**: Add `onScroll` handler to FlatList to track `isAtBottom` state
- **Empty state**: Replace `ListEmptyComponent` text with `<EmptyState>` component
- **Typing indicator**: Show as a special rendered item when streaming + last message is empty assistant message
- **Props expansion**: Receives new props for `context`, `context`, `onPromptSelect`, `selectedCandidateId` (for EmptyState) and `onNewConversation` is handled at the toolbar level

### 3. MessageBubble Enhancement

- **Assistant styling**: `bg-warm-gray` → `bg-white shadow-card`
- **Avatar**: 24x24 `bg-civic-navy` circle with `sparkles-outline` (16px, white) — displayed to the left of assistant bubbles only
- **Timestamp**: `text-xs text-text-caption` below each bubble, formatted as "HH:mm" (using `Intl.DateTimeFormat`)
- **Markdown**: Assistant messages rendered via `<Markdown>` from `react-native-markdown-display` with custom style rules matching the app's typography (Inter font, civic-navy headings, text-body for content)
- **User messages**: Continue rendering as plain `<Text>` (FR-020)

### 4. AssistantContextControls Enhancement

- **Icons**: Add `<Ionicons>` before each label — `book-outline` / `chatbubble-ellipses-outline` / `scale-outline`
- **Text size**: `text-xs` → `text-sm` for readability
- **Layout**: `flex-row items-center gap-1` within each tab to accommodate icon + text
- **Remove `numberOfLines={1}`** — with icons the labels should fit; if not, allow 2 lines

### 5. ContextPrompts Redesign

- **Border**: `border border-warm-gray` → `border border-accent-coral`
- **Background**: `bg-warm-gray` → `bg-warm-white`
- **Icon**: Add `<Ionicons name="arrow-forward" size={12}>` at right edge of each chip
- **Layout**: Vertical stack → `flex-row flex-wrap gap-2`
- **Touch target**: Maintain minimum 44px height

### 6. New Dependency

```json
{
  "react-native-markdown-display": "^7.0.2"
}
```

- Pure JavaScript — no native modules, no pod install, no ejecting
- Custom styles via `StyleSheet.create()` matching app typography:
  - `heading1`: `fontFamily: 'SpaceGrotesk_600SemiBold'`, `color: '#1B2A4A'`, `fontSize: 18`
  - `heading2`: `fontFamily: 'SpaceGrotesk_500Medium'`, `color: '#1B2A4A'`, `fontSize: 16`
  - `body`: `fontFamily: 'Inter_400Regular'`, `color: '#3D3D3D'`, `fontSize: 16`
  - `strong`: `fontFamily: 'Inter_500Medium'`
  - `bullet_list`, `ordered_list`: standard indentation
  - `link`: `color: '#E8553A'` (accent-coral)

## Complexity Tracking

> No constitution violations — this section is intentionally empty.
